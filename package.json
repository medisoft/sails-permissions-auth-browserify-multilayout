{
  "name": "sails-generate-sails-permissions-auth-browserify-multilayout",
  "version": "0.10.2",
  "description": "sails generate sails-permissions-auth-browserify-multilayout",
  "main": "Generator.js",
  "scripts": {
    "test": "mocha"
  },
  "keywords": [
    "sails-permissions-auth-browserify-multilayout",
    "generator",
    "sails",
    "generate",
    "plugin"
  ],
  "author": "balderdashy",
  "license": "MIT",
  "dependencies": {
    "lodash": ">=2.4.x",
    "merge-defaults": ">=0.1.0",
    "sails-auth": "^2.1.3",
    "sails-permissions": "git+https://github.com/medisoft/sails-permissions.git"
  },
  "devDependencies": {
    "sails-generate": "*",
    "reportback": "*",
    "fs-extra": "*"
  },
  "sailsGenerator": {
    "type": "sails-permissions-auth-browserify-multilayout",
    "behavior": "overrides `sails generate sails-permissions-auth-browserify-multilayout`",
    "sailsVersion": "~0.10.0"
  }
}
